# HyperFrames runtime caches
.hyperframes/
.thumbnails/
.waveform-cache/

# Render outputs
renders/

# Generated narration (re-created by `npm run tts`)
lib/voiceover/narration.wav

# VibeVoice opt-in (heavy — installed per machine via scripts/vibevoice/setup.sh)
.venv-vibevoice/
vendor/

# macOS / editor cruft
.DS_Store
*.swp
*.swo
